using System;
using UnityEngine;

public class PowerUpBoostParticle  {
	
/*	private Ship ship;
	
	private static float ALPHA_MIN = 0.2f;
	private static float ALPHA_MAX = 0.8f;
	
	private static float SIZE_MIN = 1.0f;
	private static float SIZE_MAX = 2.0f;
	
	private Vector3 sizeFrom;
	private Vector3 sizeTo;
	
	private Vector3 sizeStart;
			
	public void Initialize(Ship ship_) {
		ship = ship_;
		sizeStart = transform.localScale;
		sizeFrom = SIZE_MIN * sizeStart;
		sizeTo = SIZE_MAX * sizeStart;
	}
		
	void FixedUpdate() {
		transform.LookAt(ship.transform.position, ship.transform.up);
		transform.localScale = Vector3.Lerp(sizeFrom, sizeTo, Math.Abs(Mathf.Sin(Time.fixedTime)));
		//Debug.Log (Mathf.Sin(Time.fixedTime));
	}*/

}
